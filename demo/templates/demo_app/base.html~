<html>
  <head>
  </head>
  <body>
    <h1> Positive Feedback</h1>
    {% for tweet in positive_tweets %}
    <div class="positive-tweet">
      {{tweet.user_name}} - {{tweet.text}}
    </div>
    { % endfor % }
    <h1> Negative Feedback </h1>
    {% for tweet in negative_tweets %}
    <div class="positive-tweet">
      {{tweet.user_name}} - {{tweet.text}}
    </div>
    { % endfor % }
  </body>
</html>
